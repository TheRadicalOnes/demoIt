# Exact versions only. npm supply-chain compromises are frequent enough, and demoIt runs on every
# page the user visits, that an unattended transitive upgrade is not worth automatic patch bumps.
save-exact=true
